import React from 'react';

import './styles.scss';

import FilterScene from '../../components/filter-scene';

const App: React.FC = () => {
  return (
    <div className="app-container">
      <FilterScene />
    </div>
  );
};

export default App;
